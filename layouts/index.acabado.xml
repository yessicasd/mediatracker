<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Acabados on {{ .Site.Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>Items con estado 'Acabado' en {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>{{ site.Language.LanguageCode }}</language>{{ with .Site.Author.email }}
    <managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Author.email }}
    <webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS_ACABADO" -}}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end -}}
    {{ range where .Site.RegularPages "Params.status" "eq" "Acabado" }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      <description>{{ .Summary | html }}</description>
      <status>{{ .Params.status }}</status>
      {{ if .Params.image }}
        {{ $img := .Resources.GetMatch .Params.image }}
        {{ if $img }}
          {{ $img = $img.Resize "600x webp" }}
          <enclosure url="{{ $img.Permalink }}" length="{{ len $img.Content }}" type="image/webp" />
        {{ else }}
          {{ $imgUrl := .Params.image | absURL }}
          <enclosure url="{{ $imgUrl }}" type="image/jpeg" />
        {{ end }}
      {{ end }}
    </item>
    {{ end }}
  </channel>
</rss>
